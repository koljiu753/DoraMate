# 02 - Dora 项目现有架构分析

> 本章内容来自原版文档，深入理解 DORA 的核心架构是成功开发 DoraMate 的基础。

---

## 📊 2.1 核心架构特点

基于对代码库的分析，DORA 框架具有以下核心特性：

### 数据流范式

**声明式配置**
- 通过 YAML 文件定义数据流（`dataflow.yml`）
- 配置简洁、易于版本控制
- 支持模块化设计

**节点隔离**
- 每个节点作为独立进程运行
- 故障隔离、稳定性高
- 支持分布式部署

**输入输出映射**
- 节点间通过输入输出进行数据传递
- 支持多种映射类型（User、Timer、External）
- 灵活的数据路由

**定时器支持**
- 内置 `dora/timer/millis/{interval}` 定时器输入
- 支持精确的时间控制
- 适用于周期性数据采集

### 数据流结构

```yaml
nodes:
  - id: node_id              # 节点唯一标识
    build: install_command    # 可选：依赖安装命令
    path: executable_path     # 节点可执行文件路径
    inputs:                  # 输入映射
      input_id: source/output
      tick: dora/timer/millis/20
    outputs:                 # 输出列表
      - output_id
    env:                     # 环境变量
      KEY: value
```

### 通信机制

**本地通信: 共享内存（零拷贝）**
- 极致性能：比传统 IPC 快 10-17 倍
- 使用共享内存段传输数据
- 自动丢弃过时消息

**分布式通信: Zenoh 协议（2025年新增）**
- 支持跨设备数据流
- 基于高性能的 Zenoh 协议
- 与本地通信无缝切换

**消息格式: Apache Arrow**
- 跨语言零拷贝数据交换
- 列式存储、高效序列化
- 支持 Python、Rust、C、C++、C#

**遥测: OpenTelemetry 支持**
- 分布式追踪
- 性能监控
- 集成 Prometheus/Grafana

---

## 🛠️ 2.2 现有技术栈

| 层级 | 技术栈 | 说明 |
|-----|--------|------|
| **核心框架** | Rust | 100% Rust 实现，极致性能 |
| **节点 API** | Python, Rust, C, C++, C# | 多语言支持，通过 C FFI 通信 |
| **配置管理** | YAML + Serde | 声明式数据流定义 |
| **消息格式** | Apache Arrow | 跨语言零拷贝数据交换 |
| **CLI 工具** | `dora-cli` | 构建和运行数据流 |
| **守护进程** | `dora-daemon` | 共享内存管理、节点协调 |
| **运行时** | `dora-runtime` | 单个节点执行环境 |

### 核心组件详解

#### dora-core - 核心库
- `Descriptor`: 数据流描述符解析
- `Node`: 节点配置定义
- `Input/Output`: 输入输出映射
- `Validation`: 数据流验证逻辑

#### dora-message - 消息类型
- `NodeRunConfig`: 节点运行配置
- `InputMapping`: 输入映射（User/Timer）
- `OperatorConfig`: 操作符配置

#### dora-cli - 命令行工具
- `dora build`: 安装节点依赖
- `dora run`: 运行数据流
- `dora template`: 生成模板

#### dora-daemon - 守护进程
- 共享内存段管理
- 节点生命周期管理
- 数据流协调

---

## 🔌 2.3 可复用的核心组件

### 1. dora-core

**核心功能**:
- 解析 YAML 数据流描述符
- 验证节点配置
- 生成数据流运行计划

**关键类型**:
```rust
pub struct Descriptor {
    pub nodes: Vec<Node>,
}

pub struct Node {
    pub id: String,
    pub path: String,
    pub build: Option<String>,
    pub inputs: HashMap<String, InputMapping>,
    pub outputs: Vec<String>,
    pub env: Option<HashMap<String, String>>,
}
```

### 2. dora-message

**独立版本控制** (当前 0.6.0)

**核心消息类型**:
```rust
pub enum InputMapping {
    User(UserInput),
    Timer(TimerInput),
    External(ExternalInput),
}

pub struct UserInput {
    pub source: String,
    pub output: Option<String>,
    pub transform: Option<Transform>,
}

pub struct TimerInput {
    pub interval_sec: f64,
    pub initial_offset_sec: Option<f64>,
}
```

### 3. dora-cli

**核心命令**:

```bash
# 构建数据流（安装依赖）
dora build ./dataflow.yml --uv

# 运行数据流
dora run ./dataflow.yml --uv

# 生成模板
dora template my-dataflow
```

**与 DoraMate 的集成点**:
- 通过进程调用 `dora-cli`
- 捕获标准输出和错误输出
- 解析执行结果

---

## 🌐 2.4 语言绑定层次

DORA 支持多种语言，通过统一的 C FFI 层实现：

```
┌─────────────────────────────────────┐
│         Python (PyO3)               │
│         C# (P/Invoke)              │
│         C++ (C FFI)                 │
└──────────────┬──────────────────────┘
               │
┌──────────────▼──────────────────────┐
│         C API (FFI Layer)           │
└──────────────┬──────────────────────┘
               │
┌──────────────▼──────────────────────┐
│         Rust Core                   │
└─────────────────────────────────────┘
```

### 各语言特性对比

| 语言 | 节点 API | 操作符 API | 特点 |
|------|---------|-----------|------|
| **Rust** | ✅ 完整 | ✅ 完整 | 原生实现、性能最优 |
| **Python** | ✅ 完整 | ✅ 完整 | PyO3 绑定、同步/异步 |
| **C** | ✅ 完整 | ✅ 完整 | FFI 层、所有语言基础 |
| **C++** | ✅ 完整 | ✅ 完整 | 通过 C FFI |
| **C#** | ✅ 完整 | ✅ 完整 | P/Invoke 调用 C API |

---

## 📦 2.5 dora-hub 节点生态

### 节点注册表

**官方节点库**:
- 输入节点：Camera、Microphone、File Reader
- 处理节点：YOLO Detection、SAM2 Segmentation、Qwen2.5 LLM
- 输出节点：Rerun Viewer、Video Recorder

**节点元数据**:
```json
{
  "id": "opencv-video-capture",
  "name": "Camera Capture",
  "description": "从摄像头捕获视频流",
  "category": "input",
  "language": "python",
  "repository": "https://github.com/dora-rs/dora-hub",
  "inputs": {
    "tick": {
      "source": "dora/timer/millis/20"
    }
  },
  "outputs": ["image"]
}
```

### 与 DoraMate 的集成

**节点元数据获取**:
```rust
// 从 dora-hub 获取节点列表
use reqwest::Client;

async fn fetch_nodes() -> Result<Vec<DoraHubNode>, Error> {
    let client = Client::new();
    let response = client
        .get("https://raw.githubusercontent.com/dora-rs/dora-hub/main/node-hub/registry.json")
        .send()
        .await?;

    let nodes = response.json::<Vec<DoraHubNode>>().await?;
    Ok(nodes)
}
```

**节点面板渲染**:
- 根据 `category` 分类显示
- 使用 `language` 显示图标
- 根据 `inputs`/`outputs` 生成端口

---

## 🎯 2.6 2025年重要更新

根据项目 README，以下是 2025 年的一些重要功能更新：

### 2025/08
- `dora.builder` Python API - 用于以命令式方式定义数据流的新 Pythonic API

### 2025/07
- Kornia Rust 节点 - 支持 V4L/Gstreamer 相机和 Sobel 图像处理

### 2025/06
- 基于 Git 的节点支持
- `dora-vggt` 多相机深度估计

### 2025/05
- `dora-pytorch-kinematics` (FK/IK)
- `dora-mediapipe` 姿态估计
- `dora-rustypot` 串口通信

### 2025/04
- `dora-cotracker` 点追踪
- `dora-rav1e` AV1 编码（最高 12bit）
- `dora-dav1d` AV1 解码

### 2025/03
- **Python 异步 API 支持** ⭐
- Microsoft Phi4/Magma 模型支持
- Meta SAM2
- **Zenoh 分布式数据流** ⭐

### 2025/02
- Qwen2.5 (LLM)
- Qwen2.5-VL (VLM)
- outetts (TTS) 支持

**这些更新反映了框架正在积极发展**，特别是在 AI 模型集成、分布式通信和开发者体验方面。

---

## 💡 2.7 对 DoraMate 的启示

### 1. 架构设计启示

**YAML 是核心**
- DoraMate 必须完美解析和生成 YAML
- 需要支持所有 DORA 的 YAML 特性
- 保持与 DORA CLI 100% 兼容

**节点是第一公民**
- 节点元数据管理是核心
- 需要建立节点注册表
- 支持自定义节点上传

**多语言支持**
- DoraMate 需要识别节点语言类型
- 不同语言的节点可能有不同的配置需求

### 2. 技术集成点

**与 dora-cli 集成**
- 运行数据流：调用 `dora run`
- 构建数据流：调用 `dora build`
- 验证数据流：调用 `dora-daemon` API

**与 dora-hub 集成**
- 获取节点元数据
- 显示节点文档
- 一键安装节点

**与 Apache Arrow 集成**（可选）
- 未来可能需要直接处理 Arrow 数据
- 用于实时数据预览

### 3. 用户体验优化

**降低 YAML 复杂度**
- 提供可视化节点配置
- 自动生成输入映射
- 智能推荐连接

**实时反馈**
- 显示节点运行状态
- 实时日志输出
- 错误提示和建议

---

## 🔧 2.8 Rust 技术栈的优势 ⭐

### 为什么 DORA 选择 Rust？

**1. 极致性能**
```rust
// 零拷贝通信示例
use dora_message::NodeRunConfig;

// 无序列化开销，直接内存共享
pub fn process_message(message: &ArrowArray) -> Result<Output> {
    // 直接操作共享内存中的数据
    let data = message.data();
    // ... 处理逻辑
}
```

**2. 内存安全**
- 编译时保证内存安全
- 无垃圾回收（GC）
- 无数据竞争

**3. 跨平台编译**
- 一次编写，多平台运行
- WebAssembly 支持
- 嵌入式设备支持

**4. 丰富的生态系统**
- Apache Arrow (数据处理)
- Tokio (异步运行时)
- Serde (序列化)
- Trunk (WASM 构建工具)

### DoraMate 与 DORA 的技术协同 ⭐

**全栈 Rust 优势**:
1. **类型安全**: 前后端共享数据类型定义
2. **代码复用**: 直接复用 dora-core 和 dora-message 类型
3. **性能一致性**: 统一的零拷贝通信模式
4. **工具链统一**: Cargo 管理所有依赖

**示例：共享数据类型**
```rust
// 共享类型定义 (前后端共用)
// types/src/node.rs

use serde::{Deserialize, Serialize};
use dora_message::InputMapping;

#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct DoraNode {
    pub id: String,
    pub path: String,
    pub inputs: Vec<DoraInput>,
    pub outputs: Vec<String>,
}

#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct DoraInput {
    pub id: String,
    pub mapping: InputMapping,  // 直接复用 DORA 类型
}

// 前端 (Leptos)
use leptos::*;
use types::DoraNode;

#[component]
pub fn NodePanel(node: DoraNode) -> impl IntoView {
    view! {
        <div class="node">
            <h3>{&node.id}</h3>
            // ... 渲染逻辑
        </div>
    }
}

// 后端 (Axum)
use axum::Json;
use types::DoraNode;

pub async fn get_nodes() -> Json<Vec<DoraNode>> {
    // 直接使用 DORA 类型
    Json(nodes)
}
```

---

## 🚀 下一步

**理解了 DORA 架构后，继续阅读**：

- 📖 [03 - 产品定位与架构设计](./03-产品定位与架构设计.md) - 了解 DoraMate 的定位
- 🛠️ [09 - Dora 本地集成](./09-Dora本地集成.md) - 详细集成方案
- 🔧 [10 - YAML 可视化功能](./10-YAML可视化功能.md) - 核心功能实现

---

**文档作者**: 夏豪
**最后更新**: 2025-01-22
**版本**: v4.0 (Rust 全栈 + 纯文件系统)
**基于**: 原版 02-Dora架构分析.md + DoraMate 技术实现路径
